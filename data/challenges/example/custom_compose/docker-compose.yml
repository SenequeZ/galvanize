services:
  web:
    image: nginx:alpine
    environment:
      FLAG: "flag{example_custom}"
    depends_on:
      - db
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: secret
